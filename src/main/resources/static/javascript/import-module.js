//Allow vue in browser dev extension to persist 
Vue.config.devtools = true;

//Add required vuelidate keyword identifiers here to be used
const { required, minLength, between, sameAs} = window.validators;

